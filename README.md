# Multi-Container Voting App
A Dockerised voting application built with Flask, Node.js, and PostgreSQL.

## Setup
1. Clone the repo
2. Copy `.env.example` to `.env` and fill in your values
3. Run `docker-compose up --build`
